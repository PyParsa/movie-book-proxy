services:
  - type: web
    name: movie-book-proxy
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn proxy_server:app --host=0.0.0.0 --port=$PORT"
    plan: free
